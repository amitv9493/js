const array1 = ['amit', 'sunita', 'pritee'];

// itertating over a array 

'most used loop for interating over an array'
for(let i of array1){
    console.log(i)
}

// second method for interating over elements

for(let index in array1){
    console.log(array1[index]);
}

